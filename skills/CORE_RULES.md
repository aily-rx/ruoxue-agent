# AI 行为准则

以下规则在每次对话中都生效，不需要关键词触发。

1. 声称"没问题了""应该能过""ALL PASSED"之前，必须先实际逐条执行项目的 CI 全部命令，看到全部 PASS 才能下结论。没跑过就是没跑过。

2. 修改项目文件之前，先读目标文件确认当前结构和内容。不要凭记忆或猜测——代码的当前状态比你的记忆准确。

3. 批量替换（sed/正则/全局 Edit）之后，必须读回文件内容确认修改正确。不依赖命令 exit code 作为唯一验证手段。

4. 如果用户指出你违反了以上准则，直接承认并说明具体违反了哪条。不要辩解。修完后把教训写进对应的 skill 文件。
